Continue working on altering reg_wsrf_tools to 
work with latest form of secure api. - TEST

Remove some unecessary malloc's from the
WSRF steering code.

Make the Msg_type an ENUM?

Doxygen for reg_steer_tools?

update launcher manual?

Continue work on wrapping proxy IO - ACKS with a proxy??

Investigate use of gSoap to generate XML parsing code

Investigate libxml's configure scripts to see if can implement for
the steering lib.

flickering legend in history plots
